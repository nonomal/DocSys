<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.DocSystem.agent.repository.AgentConfigRepository">

    <resultMap id="AgentConfigEntityResultMap" type="com.DocSystem.agent.config.AgentConfigEntity">
        <id property="id" column="id"/>
        <result property="scope" column="scope"/>
        <result property="configKey" column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.DocSystem.agent.config.AgentConfigEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_config (scope, config_key, config_value, updated_at)
        VALUES (#{scope}, #{configKey}, #{configValue}, #{updatedAt})
    </insert>

    <update id="update">
        UPDATE agent_config
        SET config_value = #{configValue}, updated_at = CURRENT_TIMESTAMP
        WHERE scope = #{scope} AND config_key = #{configKey}
    </update>

    <select id="selectByKey" resultMap="AgentConfigEntityResultMap">
        SELECT * FROM agent_config
        WHERE scope = #{scope} AND config_key = #{configKey}
        LIMIT 1
    </select>

    <select id="selectByScope" resultMap="AgentConfigEntityResultMap">
        SELECT * FROM agent_config
        WHERE scope = #{scope}
        ORDER BY config_key ASC
    </select>

</mapper>
