<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.DocSystem.agent.learning.repository.UserCustomLlmModelRepository">

    <resultMap id="UserCustomLlmModelResultMap" type="com.DocSystem.agent.learning.entity.UserCustomLlmModel">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="name" column="name"/>
        <result property="modelName" column="model_name"/>
        <result property="endpoint" column="endpoint"/>
        <result property="apiKey" column="api_key"/>
        <result property="settings" column="settings"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUserId" resultMap="UserCustomLlmModelResultMap">
        SELECT * FROM user_custom_llm_models WHERE user_id = #{userId} ORDER BY id
    </select>

    <select id="findByIdAndUserId" resultMap="UserCustomLlmModelResultMap">
        SELECT * FROM user_custom_llm_models WHERE id = #{id} AND user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.DocSystem.agent.learning.entity.UserCustomLlmModel"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_custom_llm_models
            (user_id, tenant_id, name, model_name, endpoint, api_key, settings, created_at, updated_at)
        VALUES
            (#{userId}, #{tenantId}, #{name}, #{modelName}, #{endpoint}, #{apiKey}, #{settings}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateByIdAndUserId" parameterType="com.DocSystem.agent.learning.entity.UserCustomLlmModel">
        UPDATE user_custom_llm_models SET
            name = #{name},
            model_name = #{modelName},
            endpoint = #{endpoint},
            api_key = #{apiKey},
            settings = #{settings},
            updated_at = #{updatedAt}
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <delete id="deleteByIdAndUserId">
        DELETE FROM user_custom_llm_models WHERE id = #{id} AND user_id = #{userId}
    </delete>

</mapper>
