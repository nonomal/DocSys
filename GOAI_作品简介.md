# GOAI 世界人工智能开源大赛 · 初赛提交 · 作品简介

> 赛道：新智基座（Agent Infra） | 提交截止：2026-08-16 | 项目开源：https://gitee.com/RainyGao/DocSys

---

## 项目名称

**MxsDoc AI：基于 AgentTeams 的企业文档多 Agent 协同基座**

（约 20 字）

---

## 正文（498 字）

**问题与场景**：企业文档管理系统中，用户希望用自然语言完成"查找文档、创建仓库、上传文件、全文检索、知识问答"等操作——但单 Agent 在真实生产环境中"水土不服"：缺权限隔离、缺操作审计、缺稳定接入、缺可观测。实验室能跑的 Demo，进了企业系统就失控。

**核心解决方案**：MxsDoc AI 构建在真实开源文档系统 MxsDoc（DocSystem）之上，采用 **AgentTeams 协同架构**——5 个职能 Agent 分工协作，实现端到端任务闭环：

1. **编排 Agent（MainAgent）**：意图识别 → 任务拆解 → 分派 → 结果聚合，生命周期统一收口；
2. **执行 Agent（SubAgent 池）**：将子任务映射到 25 个企业级工具（JSON Schema 定义，含 13 个只读 + 12 个写工具），LLM 自主多轮调用，支持流式推理过程可视化；
3. **反思 Agent**：执行失败时 LLM 诊断 → 重规划 → 有上限重跑，实现异常自愈；
4. **进化 Agent**：从成功经验中"结晶"出可复用 Skill（SKILL.md 标准），实现能力越用越强；
5. **推荐 Agent**：行为标签 + 协同过滤，相似场景推荐最优策略。

基座为 **ToolUseLoop 推理引擎**（REPL 循环）：LLM 作为推理引擎自主决定调工具 → 观察结果 → 再推理 → 最多 10 轮直至给出最终答案，对齐 Claude Code / Copilot 工作方式。

**创新点与差异化**：① 真实企业系统底座——Agent 与文档系统共享会话、权限、审计，天然生产级；② 完整安全闭环——写操作 SSE 二次确认 + SHA-256 脱敏审计 + Bucket4j 令牌桶限流；③ **自进化 Skill 工程体系**——接口 + 优先级 + Spring 自动装配的可插拔架构，内置与外部（Python/CLI/LLM 三段式）双级执行，对齐 SKILL.md 开放标准；④ 可观测能力——Micrometer 指标 + 健康探针 + MDC 结构化日志 + Step 审计，249+ 单元测试全绿。

**开放/复用价值**：已 Gitee 开源；Skill 执行器可插拔、SKILL.md 标准可复用，任何企业系统均可按此模式接入多 Agent 能力。

**当前进展**：ToolUseLoop 流式推理引擎已为主路径，25 个工具端到端验证通过，Memory / Web Search / Step 审计 / 管理后台提示词配置均已实装，249+ 单元测试全部通过，可运行可演示。
