[
  {
    "id": 1,
    "prompt": "login as admin with password password123",
    "expected": "Skill triggers and calls docsys login admin password123. Returns login success with username, role, and session status. Does not echo the password in output."
  },
  {
    "id": 2,
    "prompt": "log me in as zhangsan",
    "expected": "Skill triggers but password is missing — skill should prompt for password before calling the command. Handles Invalid credentials with a user-friendly message (not exposing which field was wrong)."
  }
]
