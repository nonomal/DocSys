[
  {
    "id": 1,
    "prompt": "show system config",
    "expected": "Skill triggers and calls docsys system-config --list (or with --get if no flag needed). Returns all configuration keys and values. Handles Permission denied gracefully if user is not admin."
  },
  {
    "id": 2,
    "prompt": "set max upload size to 200",
    "expected": "Skill triggers and calls docsys system-config --set max.upload.size 200. Returns confirmation that config was updated. Rejects the request with clear error if user lacks admin permission."
  }
]
