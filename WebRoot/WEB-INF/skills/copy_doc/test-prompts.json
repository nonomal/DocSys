[
  {
    "id": 1,
    "prompt": "copy document 123 to folder 5 in repo 1",
    "expected": "Document 123 copied to folder 5 in repo 1. Returns brief confirmation with the new document ID. Auto-renames if a file with the same name already exists in the target."
  },
  {
    "id": 2,
    "prompt": "cp doc 789 to folder backup",
    "expected": "Recognizes cp alias, extracts docId=789, but cannot resolve 'folder backup' to a numeric targetPid. Should either prompt the user for the folder ID or return a clear error requesting the numeric folder ID."
  },
  {
    "id": 3,
    "prompt": "复制文档 456 到根目录",
    "expected": "Correctly parses docId=456 and targetPid=0 (root). Returns copy result with new document ID. Chinese confirmation output."
  }
]
